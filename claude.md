## Verification Workflow

- Do NOT run `tsc` / type-check after making edits.
- Do NOT start the Vite dev server (`npm run dev` / `vite`) after making edits.
- Do NOT run the app or open a browser to verify changes unless I explicitly ask you to.
- After editing code, just stop and summarize what you changed. I will run type-check and the dev server myself.
- Only run build/type-check/dev-server commands if I explicitly say "verify this" or "run type-check."
